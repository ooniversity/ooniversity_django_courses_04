{% extends 'base.html' %}
{% load staticfiles %}


{% block title %}Main PyBursa{% endblock title %}


{% block nav_class_index %}active{% endblock %}


{% block main_content %}
    <div class="main_font">

      {% for course in courses %}
      <h1><a href="{% url 'students:list_view' %}?course_id={{course.id}}">{{course.name}}</a></h1>        
        <p>{{course.description|truncatechars:35}}</p>      
          <div class="container-fluid">


{% include 'include/coach_include.html' with block_title='Учитель' instructor=course.coach %}

{% include 'include/coach_include.html' with block_title='Ассистент' instructor=course.assistant %}
        
         {% endfor %}             
             
         
          <h2>План курса</h2>     
          <table class="table">
            <thead class="table-bordered">
              <tr>
                <th>#</th>
                <th>Тема</th>
                <th>Описание</th>
              </tr>
            </thead>
            <tbody class="table-bordered">
              {% for lesson in lessons %}
                <tr>
                  <td>{{lesson.order}}</td>
                  <td>{{lesson.subject}}</td>
                  <td>{{lesson.description}}</td>
                </tr>
              {% endfor %}             
            </tbody>
          </table>

        </div>          
      </div>
{% endblock main_content %}
