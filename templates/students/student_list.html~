{% extends "base.html" %}


{% block title %}List of students{% endblock title %}


{% block nav_class_student %}active{% endblock %}


{% block content %}
	{% if messages %}
	<!--<ul class="messages">
		{% for message in messages %}
		<li{% if message.tags %} class="{{ message.tags }}"{% endif %}>{{ message }}</li>
		{% endfor %}
	</ul>-->
		{% for message in messages %}
			<div class="alert alert-success" role="alert">{{ message }}</div>
		{% endfor %}
	{% endif %}
 
	<button class="btn btn-primary" type="button"><a href="{% url 'students:add' %}" style="color:white">
		<span class="glyphicon glyphicon-plus"></span>Добавить нового студента</a></button> 
	<h3>Список студентов</h3>
	  <table class="table">
		<thead id="id2">
		  <tr>
		   <td><b>#</b></td>
		   <td><b>фамилия имя<b></td>
		   <td><b>адрес<b></td>
		   <td><b>skype<b></td>
		   <td><b>курсы<b></td>
		  </tr>
		</thead>
		<tbody>		
		{% for i in students %}		
		  <tr>
			<td>{{ forloop.counter }}</td>
		    <td><a href="{% url 'students:detail' i.id %}">{{ i }}</a></td>
		    <td>{{ i.address }}</td>
			<td>{{ i.skype }}</td>
			<td>			
			  {% for course in i.courses.all %}
			  <a href="{% url 'courses:detail' course.id %}">{{ course.name }}<br></a>
			  {% endfor %}
			</td>
			<td><button class="btn btn-primary" type="button">
				<a href="{% url 'students:edit' i.id %}" style="color:white">
				<span class="glyphicon glyphicon-pencil"></span> Изменить</a></button>
			</td>
			<td><button class="btn btn-primary" type="button">
				<a href="{% url 'students:remove' i.id %}" style="color:white">
				<span class="glyphicon glyphicon-remove"></span> Удалить</a></button>
			</td>
		  </tr>
		{% endfor %}			  
	   </tbody>	
	  </table>
{% endblock content %}	
