# -*- coding: utf-8 -*-
from django.shortcuts import  render

def views.quadratic_results(request):
	d =b ** 2 - 4 * a * c
	if d < 0:
	    print 'roots of the equation does exist'
	elseif d ==0:
	   x = -b / 2*a
		print 'There is one equation root x1=x2=%g' %x
	else:
		x1 = (-b +d **(1/2.0) / (2*a)
		x1 = (-b -d **(1/2.0) / (2*a)
	    print 'There are roots of equation root x1= %g x2=%g' (x1, x2)


    return render(request,'results.html')



